node_modules/
dist/
.next/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/

# 원본 문서 번들 — docs/ 에 동일 내용이 있다
files (1).zip
files (1)/

.vercel
.env*
!.env.example

# vercel curl 이 -w 플래그를 파일명으로 해석해 만드는 산출물
-w

# Supabase CLI 가 만드는 로컬 상태 — 링크된 프로젝트 ref 뿐이다
supabase/.temp/
